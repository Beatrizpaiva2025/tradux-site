# MongoDB (same as Legacy — uses separate DB "tradux")
MONGO_URL="mongodb+srv://user:pass@cluster.mongodb.net/"
DB_NAME="tradux"

# CORS
CORS_ORIGINS="https://tradux.online,https://tradux-site.vercel.app,http://localhost:5173"

# Stripe (same keys as Legacy — orders separated by metadata)
STRIPE_API_KEY="sk_test_your_stripe_key"
STRIPE_WEBHOOK_SECRET="whsec_your_webhook_secret"

# Email (same Resend key — sender differs)
RESEND_API_KEY="re_your_resend_key"
SENDER_EMAIL="TRADUX <orders@tradux.online>"
ADMIN_EMAIL="contact@tradux.online"

# AI Translation (Anthropic Claude)
ANTHROPIC_API_KEY="sk-ant-your_key"

# AWS Textract (OCR - optional, falls back to Tesseract)
AWS_ACCESS_KEY_ID=""
AWS_SECRET_ACCESS_KEY=""
AWS_REGION="us-east-1"
